// User 相关枚举 (v6版本)

// User 状态
export enum UserStatus {
  ENABLED = 0,   // 启用
  DISABLED = 1,  // 禁用
}

// User Follow 状态
export enum UserFollowStatus {
  ENABLED = 0,   // 启用
  DISABLED = 1,  // 禁用
}
